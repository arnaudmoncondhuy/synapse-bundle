{#
    Layout de base pour l'administration du bundle Synapse.

    Ce layout est minimaliste et agnostique pour permettre
    au projet de le surcharger facilement.

    Variables attendues:
    - module_color: Couleur du module
    - module_icon: Icône du module
    - nav_active: Page active dans la navigation
#}
<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Administration Synapse{% endblock %}</title>
    {% block stylesheets %}{% endblock %}
</head>
<body>
    <div class="synapse-admin">
        {% block admin_navigation %}
            <nav class="synapse-admin-nav">
                <a href="{{ path(admin_route_prefix ~ '_dashboard') }}" class="{{ nav_active == 'dashboard' ? 'active' : '' }}">
                    Dashboard
                </a>
                <a href="{{ path(admin_route_prefix ~ '_risks') }}" class="{{ nav_active == 'risks' ? 'active' : '' }}">
                    Risques
                </a>
                <a href="{{ path(admin_route_prefix ~ '_analytics') }}" class="{{ nav_active == 'analytics' ? 'active' : '' }}">
                    Analytique
                </a>
                <a href="{{ path(admin_route_prefix ~ '_config') }}" class="{{ nav_active == 'config' ? 'active' : '' }}">
                    Configuration
                </a>
            </nav>
        {% endblock %}

        <main class="synapse-admin-content">
            {% for message in app.flashes('success') %}
                <div class="alert alert-success">{{ message }}</div>
            {% endfor %}

            {% for message in app.flashes('error') %}
                <div class="alert alert-error">{{ message }}</div>
            {% endfor %}

            {% block admin_content %}{% endblock %}
        </main>
    </div>

    {% block javascripts %}{% endblock %}
</body>
</html>
