{% extends synapse_admin_layout() %}

{% block admin_title %}Configuration - Synapse Admin
{% endblock %}

{% block admin_content %}
	<header class="synapse-admin__header">
		<div>
			<h1 class="synapse-admin__title">Configuration</h1>
			<p class="synapse-admin__subtitle">Paramètres globaux de l'assistant et du modèle IA</p>
		</div>
	</header>

	<div style="max-width: 1000px; padding-bottom: 4rem;">
		<form
			method="post">

			{# ============================= MODÈLE IA ============================= #}
			<div class="synapse-admin__card" style="margin-bottom: 2rem;">
				<div class="synapse-admin__card-header">
					<div class="synapse-admin__kpi-icon synapse-admin__kpi-icon--primary" style="width: 32px; height: 32px; font-size: 1rem; border-radius: 6px;">
						<i data-lucide="cpu"></i>
					</div>
					<div>
						<h2 style="font-size: 1.1rem; margin: 0;">Modèle IA</h2>
						<p style="margin: 0; font-size: 0.9rem; color: var(--synapse-admin-text-muted);">Sélectionnez la version du modèle Gemini à utiliser.</p>
					</div>
				</div>

				<div class="synapse-admin__card-body">
					<div class="synapse-admin__form-group">
						<label class="synapse-admin__label">Version du Modèle</label>
						<input type="text" name="gemini_model" class="synapse-admin__input" value="{{ config.model }}" placeholder="ex: gemini-2.5-flash" required>
						<p class="synapse-admin__help">Référez-vous à la documentation Vertex AI pour les modèles disponibles.</p>
					</div>
				</div>
			</div>

			{# ============================= VERTEX AI ============================= #}
			<div class="synapse-admin__card" style="margin-bottom: 2rem;">
				<div class="synapse-admin__card-header">
					<div class="synapse-admin__kpi-icon synapse-admin__kpi-icon--primary" style="width: 32px; height: 32px; font-size: 1rem; border-radius: 6px;">
						<i data-lucide="cloud"></i>
					</div>
					<div>
						<h2 style="font-size: 1.1rem; margin: 0;">Vertex AI</h2>
						<p style="margin: 0; font-size: 0.9rem; color: var(--synapse-admin-text-muted);">Configuration du projet Google Cloud.</p>
					</div>
				</div>

				<div class="synapse-admin__card-body">
					<div class="synapse-admin__grid-2">
						<div class="synapse-admin__form-group">
							<label class="synapse-admin__label">ID Projet GCP</label>
							<input type="text" name="vertex_project_id" class="synapse-admin__input" value="{{ config.vertexProjectId }}" placeholder="mon-projet-gcp">
						</div>
						<div class="synapse-admin__form-group">
							<label class="synapse-admin__label">Région</label>
							<input type="text" name="vertex_region" class="synapse-admin__input" value="{{ config.vertexRegion }}" placeholder="europe-west1">
						</div>
					</div>
				</div>
			</div>

			{# ============================= PROMPT SYSTÈME ============================= #}
			<div class="synapse-admin__card" style="margin-bottom: 2rem;">
				<div class="synapse-admin__card-header">
					<div class="synapse-admin__kpi-icon synapse-admin__kpi-icon--warning" style="width: 32px; height: 32px; font-size: 1rem; border-radius: 6px;">
						<i data-lucide="message-square-quote"></i>
					</div>
					<div>
						<h2 style="font-size: 1.1rem; margin: 0;">Prompt Système</h2>
						<p style="margin: 0; font-size: 0.9rem; color: var(--synapse-admin-text-muted);">Définissez le comportement global et le contexte de l'assistant.</p>
					</div>
				</div>

				<div class="synapse-admin__card-body">
					<div class="synapse-admin__alert synapse-admin__alert--info">
						<i data-lucide="info" style="flex-shrink: 0; margin-top: 2px;"></i>
						<div>
							<strong style="display: block; margin-bottom: 0.25rem;">Variables Dynamiques</strong>
							<p style="margin: 0; font-size: 0.9rem;">Vous pouvez utiliser :
								<code>{DATE}</code>,
								<code>{NOM}</code>,
								<code>{PRENOM}</code>,
								<code>{EMAIL}</code>,
								<code>{ROLE}</code>.</p>
						</div>
					</div>

					<div class="synapse-admin__form-group">
						<label class="synapse-admin__label">Instructions Personnalisées</label>
						<textarea name="system_prompt" class="synapse-admin__textarea" rows="12" placeholder="Laissez vide pour utiliser le prompt par défaut..." style="font-family: monospace;">{{ config.systemPrompt }}</textarea>
						<p class="synapse-admin__help">Ces instructions remplacent le prompt par défaut.</p>
					</div>
				</div>
			</div>

			{# ============================= SÉCURITÉ ============================= #}
			<div class="synapse-admin__card" style="margin-bottom: 2rem;">
				<div class="synapse-admin__card-header">
					<div class="synapse-admin__kpi-icon synapse-admin__kpi-icon--error" style="width: 32px; height: 32px; font-size: 1rem; border-radius: 6px;">
						<i data-lucide="shield-alert"></i>
					</div>
					<div>
						<h2 style="font-size: 1.1rem; margin: 0;">Filtres de Sécurité</h2>
						<p style="margin: 0; font-size: 0.9rem; color: var(--synapse-admin-text-muted);">Contrôle du contenu généré (recommandé pour environnement scolaire).</p>
					</div>
				</div>

				<div class="synapse-admin__card-body">
					<div style="background: var(--synapse-admin-bg-main); padding: 1rem; border-radius: var(--synapse-admin-radius-sm); border: 1px solid var(--synapse-admin-border); margin-bottom: 1.5rem; display: flex; align-items: center; gap: 0.75rem;">
						<input type="checkbox" name="safety_enabled" id="safety_enabled" value="1" {{ config.isSafetyEnabled ? 'checked' : '' }} style="width: 1.25rem; height: 1.25rem; accent-color: var(--synapse-admin-primary);">
						<div>
							<label for="safety_enabled" style="font-weight: 600; cursor: pointer; display: block;">Activer les filtres de sécurité</label>
							<p style="margin: 0; font-size: 0.85rem; color: var(--synapse-admin-text-muted);">Bloque automatiquement les contenus inadaptés.</p>
						</div>
					</div>

					<div class="synapse-admin__form-group">
						<label class="synapse-admin__label">🛡️ Seuil par Défaut</label>
						<select name="safety_default_threshold" class="synapse-admin__select">
							{% for value, label in safety_thresholds %}
								<option value="{{ value }}" {{ config.safetyDefaultThreshold == value ? 'selected' : '' }}>{{ label }}</option>
							{% endfor %}
						</select>
					</div>

					<div class="synapse-admin__grid-2">
						<div class="synapse-admin__form-group">
							<label class="synapse-admin__label">💬 Discours Haineux</label>
							<select name="safety_hate_speech" class="synapse-admin__select">
								{% for value, label in safety_thresholds %}
									<option value="{{ value }}" {{ config.safetyHateSpeech == value ? 'selected' : '' }}>{{ label }}</option>
								{% endfor %}
							</select>
						</div>

						<div class="synapse-admin__form-group">
							<label class="synapse-admin__label">⚠️ Contenu Dangereux</label>
							<select name="safety_dangerous_content" class="synapse-admin__select">
								{% for value, label in safety_thresholds %}
									<option value="{{ value }}" {{ config.safetyDangerousContent == value ? 'selected' : '' }}>{{ label }}</option>
								{% endfor %}
							</select>
						</div>

						<div class="synapse-admin__form-group">
							<label class="synapse-admin__label">😠 Harcèlement</label>
							<select name="safety_harassment" class="synapse-admin__select">
								{% for value, label in safety_thresholds %}
									<option value="{{ value }}" {{ config.safetyHarassment == value ? 'selected' : '' }}>{{ label }}</option>
								{% endfor %}
							</select>
						</div>

						<div class="synapse-admin__form-group">
							<label class="synapse-admin__label">🔞 Contenu Sexuel</label>
							<select name="safety_sexually_explicit" class="synapse-admin__select">
								{% for value, label in safety_thresholds %}
									<option value="{{ value }}" {{ config.safetySexuallyExplicit == value ? 'selected' : '' }}>{{ label }}</option>
								{% endfor %}
							</select>
						</div>
					</div>
				</div>
			</div>

			{# ============================= GÉNÉRATION ============================= #}
			<div class="synapse-admin__card" style="margin-bottom: 2rem;">
				<div class="synapse-admin__card-header">
					<div class="synapse-admin__kpi-icon synapse-admin__kpi-icon--primary" style="width: 32px; height: 32px; font-size: 1rem; border-radius: 6px;">
						<i data-lucide="settings"></i>
					</div>
					<div>
						<h2 style="font-size: 1.1rem; margin: 0;">Configuration de Génération</h2>
						<p style="margin: 0; font-size: 0.9rem; color: var(--synapse-admin-text-muted);">Contrôle créativité et longueur des réponses.</p>
					</div>
				</div>

				<div class="synapse-admin__card-body">
					<div class="synapse-admin__grid-3">
						<div class="synapse-admin__form-group">
							<label class="synapse-admin__label">🌡️ Température</label>
							<input type="number" name="generation_temperature" class="synapse-admin__input" step="0.1" min="0" max="2" value="{{ config.generationTemperature }}">
							<p class="synapse-admin__help">0.0 (Strict) - 2.0 (Créatif)</p>
						</div>

						<div class="synapse-admin__form-group">
							<label class="synapse-admin__label">🎯 Top-P (Nucleus)</label>
							<input type="number" name="generation_top_p" class="synapse-admin__input" step="0.01" min="0" max="1" value="{{ config.generationTopP }}">
						</div>

						<div class="synapse-admin__form-group">
							<label class="synapse-admin__label">🔢 Top-K</label>
							<input type="number" name="generation_top_k" class="synapse-admin__input" min="1" value="{{ config.generationTopK }}">
						</div>

						<div class="synapse-admin__form-group">
							<label class="synapse-admin__label">📏 Limite Tokens</label>
							<input type="number" name="generation_max_output_tokens" class="synapse-admin__input" min="1" value="{{ config.generationMaxOutputTokens }}" placeholder="Défaut">
						</div>
					</div>

					<div class="synapse-admin__form-group" style="margin-top: 1.5rem;">
						<label class="synapse-admin__label">🛑 Séquences d'Arrêt</label>
						<input type="text" name="generation_stop_sequences" class="synapse-admin__input" value="{{ config.generationStopSequences|join(', ') }}" placeholder="Ex: \n\n, END">
					</div>
				</div>
			</div>

			{# ============================= THINKING ============================= #}
			<div class="synapse-admin__card" style="margin-bottom: 2rem;">
				<div class="synapse-admin__card-header">
					<div class="synapse-admin__kpi-icon synapse-admin__kpi-icon--warning" style="width: 32px; height: 32px; font-size: 1rem; border-radius: 6px;">
						<i data-lucide="brain-circuit"></i>
					</div>
					<div>
						<h2 style="font-size: 1.1rem; margin: 0;">Thinking (CoT)</h2>
						<p style="margin: 0; font-size: 0.9rem; color: var(--synapse-admin-text-muted);">Paramètres de réflexion pour les modèles compatibles (Gemini 2.0+).</p>
					</div>
				</div>

				<div class="synapse-admin__card-body">
					<div style="background: var(--synapse-admin-bg-main); padding: 1rem; border-radius: var(--synapse-admin-radius-sm); border: 1px solid var(--synapse-admin-border); margin-bottom: 1.5rem; display: flex; align-items: center; gap: 0.75rem;">
						<input type="checkbox" name="thinking_enabled" id="thinking_enabled" value="1" {{ config.isThinkingEnabled ? 'checked' : '' }} style="width: 1.25rem; height: 1.25rem; accent-color: var(--synapse-admin-primary);">
						<div>
							<label for="thinking_enabled" style="font-weight: 600; cursor: pointer; display: block;">Activer le Thinking</label>
							<p style="margin: 0; font-size: 0.85rem; color: var(--synapse-admin-text-muted);">Permet au modèle de réfléchir avant de répondre.</p>
						</div>
					</div>

					<div class="synapse-admin__form-group">
						<label class="synapse-admin__label">Budget Tokens Thinking</label>
						<input type="number" name="thinking_budget" class="synapse-admin__input" min="1024" step="1024" value="{{ config.thinkingBudget }}">
						<p class="synapse-admin__help">Minimum 1024 tokens.</p>
					</div>
				</div>
			</div>

			{# ============================= CONTEXT CACHING ============================= #}
			<div class="synapse-admin__card" style="margin-bottom: 2rem;">
				<div class="synapse-admin__card-header">
					<div class="synapse-admin__kpi-icon synapse-admin__kpi-icon--success" style="width: 32px; height: 32px; font-size: 1rem; border-radius: 6px;">
						<i data-lucide="database"></i>
					</div>
					<div>
						<h2 style="font-size: 1.1rem; margin: 0;">Context Caching</h2>
						<p style="margin: 0; font-size: 0.9rem; color: var(--synapse-admin-text-muted);">Optimisation des coûts pour les contextes volumineux.</p>
					</div>
				</div>

				<div class="synapse-admin__card-body">
					<div style="background: var(--synapse-admin-bg-main); padding: 1rem; border-radius: var(--synapse-admin-radius-sm); border: 1px solid var(--synapse-admin-border); margin-bottom: 1.5rem; display: flex; align-items: center; gap: 0.75rem;">
						<input type="checkbox" name="context_caching_enabled" id="context_caching_enabled" value="1" {{ config.isContextCachingEnabled ? 'checked' : '' }} style="width: 1.25rem; height: 1.25rem; accent-color: var(--synapse-admin-primary);">
						<div>
							<label for="context_caching_enabled" style="font-weight: 600; cursor: pointer; display: block;">Activer le context caching</label>
							<p style="margin: 0; font-size: 0.85rem; color: var(--synapse-admin-text-muted);">Réutilise le contexte sans le renvoyer à chaque requête.</p>
						</div>
					</div>

					<div class="synapse-admin__alert synapse-admin__alert--info">
						<i data-lucide="info" style="flex-shrink: 0; margin-top: 2px;"></i>
						<div>
							<strong style="display: block; margin-bottom: 0.25rem;">Information Importante</strong>
							<p style="margin: 0; font-size: 0.9rem;">Min: 2,048 tokens. TTL: 1h. Économie ~90%.</p>
						</div>
					</div>

					<div class="synapse-admin__form-group">
						<label class="synapse-admin__label">🔑 ID du Cache Vertex AI</label>
						<input type="text" name="context_caching_id" class="synapse-admin__input" value="{{ config.contextCachingId }}" placeholder="projects/..." style="font-family: monospace;">
					</div>
				</div>
			</div>

			{# ============================= INFRA & STOCKAGE ============================= #}
			<div class="synapse-admin__card" style="margin-bottom: 2rem;">
				<div class="synapse-admin__card-header">
					<div class="synapse-admin__kpi-icon synapse-admin__kpi-icon--primary" style="width: 32px; height: 32px; font-size: 1rem; border-radius: 6px;">
						<i data-lucide="database-backup"></i>
					</div>
					<div>
						<h2 style="font-size: 1.1rem; margin: 0;">Infrastructure & Stockage</h2>
						<p style="margin: 0; font-size: 0.9rem; color: var(--synapse-admin-text-muted);">Persistance, Encryption et Rétention.</p>
					</div>
				</div>

				<div class="synapse-admin__card-body">
					<h3 style="font-size: 1rem; margin-top: 1.5rem; margin-bottom: 1rem; border-bottom: 1px solid var(--synapse-admin-border); padding-bottom: 0.5rem;">Rétention</h3>
					<div class="synapse-admin__grid-2">
						<div class="synapse-admin__form-group">
							<label class="synapse-admin__label">Rétention (Jours)</label>
							<input type="number" name="retention_days" class="synapse-admin__input" min="1" value="{{ config.retentionDays }}">
						</div>
					</div>
				</div>
			</div>

			{# ============================= FONCTIONNALITÉS AVANCÉES ============================= #}
			<div class="synapse-admin__card" style="margin-bottom: 2rem;">
				<div class="synapse-admin__card-header">
					<div class="synapse-admin__kpi-icon synapse-admin__kpi-icon--error" style="width: 32px; height: 32px; font-size: 1rem; border-radius: 6px;">
						<i data-lucide="award"></i>
					</div>
					<div>
						<h2 style="font-size: 1.1rem; margin: 0;">Fonctionnalités Avancées</h2>
						<p style="margin: 0; font-size: 0.9rem; color: var(--synapse-admin-text-muted);">Suivi des coûts et détection des risques.</p>
					</div>
				</div>

				<div class="synapse-admin__card-body">
					<div class="synapse-admin__grid-2">
						<div style="background: var(--synapse-admin-bg-main); padding: 1rem; border-radius: var(--synapse-admin-radius-sm); border: 1px solid var(--synapse-admin-border); display: flex; align-items: center; gap: 0.75rem;">
							<input type="checkbox" name="risk_detection_enabled" id="risk_detection_enabled" value="1" {{ config.isRiskDetectionEnabled ? 'checked' : '' }} style="width: 1.25rem; height: 1.25rem; accent-color: var(--synapse-admin-primary);">
							<div>
								<label for="risk_detection_enabled" style="font-weight: 600; cursor: pointer; display: block;">Détection des Risques</label>
							</div>
						</div>
					</div>
				</div>
			</div>

			{# ============================= UI & ADMIN ============================= #}
			<div class="synapse-admin__card" style="margin-bottom: 2rem;">
				<div class="synapse-admin__card-header">
					<div class="synapse-admin__kpi-icon synapse-admin__kpi-icon--primary" style="width: 32px; height: 32px; font-size: 1rem; border-radius: 6px;">
						<i data-lucide="layout"></i>
					</div>
					<div>
						<h2 style="font-size: 1.1rem; margin: 0;">Interface & Context</h2>
						<p style="margin: 0; font-size: 0.9rem; color: var(--synapse-admin-text-muted);">Personnalisation de l'interface et du contexte global.</p>
					</div>
				</div>

				<div class="synapse-admin__card-body">
					<div class="synapse-admin__form-group">
						<label class="synapse-admin__label">Langue de l'interface (Context)</label>
						<input type="text" name="context_language" class="synapse-admin__input" value="{{ config.contextLanguage }}" placeholder="fr">
						<p class="synapse-admin__help">Utilisée pour le prompt système si aucune langue n'est détectée.</p>
					</div>
				</div>
			</div>

			{# ============================= ACTIONS ============================= #}
			<div style="position: sticky; bottom: 2rem; background: var(--synapse-admin-bg-surface); border: 1px solid var(--synapse-admin-border); padding: 1rem 2rem; border-radius: var(--synapse-admin-radius); display: flex; justify-content: flex-end; gap: 1rem; box-shadow: var(--synapse-admin-shadow-lg); z-index: 50;">
				<a href="{{ path('assistant_index') }}" class="synapse-admin__btn synapse-admin__btn--secondary">
					<i data-lucide="x"></i>
					Annuler
				</a>
				<button type="submit" class="synapse-admin__btn synapse-admin__btn--primary">
					<i data-lucide="save"></i>
					Enregistrer
				</button>
			</div>

		</form>
	</div>
{% endblock %}
